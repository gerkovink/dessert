#' Run a dessert recipe on an analysis or data set
dessert.lm <- function(
  object,
  recipe,
  output_format = NULL,
  output_dir    = NULL) {

  print(recipe)

  print("dessert.lm")

  return(TRUE)
}
